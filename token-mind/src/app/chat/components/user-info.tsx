// import { useUser } from "@civic/auth-web3/react";

const UserInfo = () => {
    // const { user } = useUser();

  return (
    <span>
        {
        // false && {user?.name} || "User123"
        }
        User123
    </span>
  )
}

export default UserInfo